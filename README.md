## **Playing with C++'s Compiler Intrinsics**
**Benchmarking custom SIMD-based class**

**BUILD AND RUN**                 

1. Run tests
`test.bat`
2. Run benchmarks
`benchmark.bat`
3. Build custom "driver" code
`build.bat`

*.bat files are portable to Linux!